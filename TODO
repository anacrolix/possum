* Figure out how to have RustRover limit lines to 100 characters. (It thinks I want 120 characters, but rustfmt is working on 100).
* Add an integrity check for a directory that includes:
    * Removing unused values files.
    * Vacuuming the manifest file?